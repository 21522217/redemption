specVersion: "v1beta"
serviceId: "redemption"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "redemption-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
